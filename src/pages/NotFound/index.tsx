import React from 'react';
import { Typography } from 'antd';
const { Title } = Typography;

function NotFound() {
  return <Title level={4}>You seem to have lost your way...</Title>;
}

export default NotFound;

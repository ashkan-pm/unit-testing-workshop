import React from 'react';

type Props = {
  className?: string;
};
function OTPInput({ className }: Props) {
  return (
    <div className={className} style={{ width: 300 }}>
      Test
    </div>
  );
}

export default OTPInput;
